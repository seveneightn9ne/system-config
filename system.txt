% Repositories:
sudo add-apt-repository ppa:numix/ppa         % numix window theme
sudo add-apt-repository ppa:chris-lea/node.js  % node.js
sudo apt-get update

% Packages:
sudo apt-get install zsh vim unity-tweak-tool nautilus-dropbox zsh git numix-gtk-theme numix-icon-theme numix-icon-theme-circle redshift indicator-multiload ipython python-setuptools nodejs terminator gnome-tweak-tool trash-cli graphviz htop pass openssh-server fail2ban xournal entr alacarte python-pip
% bulky ones:
sudo apt-get install texlive python-sklearn latexmk texlive-science texlive-latex-extra

% From web:
% spacemacs:
git clone --recursive http://github.com/syl20bnr/spacemacs ~/.emacs.d
% oh-my-zsh:
wget --no-check-certificate http://install.ohmyz.sh -O - | sh
chsh -s /usr/bin/zsh
% log out/in
% Eclipse, Java 8, Scala -> download into /opt/

cp .vimrc ~/.vimrc
cp .zshrc ~/.zshrc %gots to be after omzsh install

% Vundle:
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

% Set up Vundle.
% Open vim, run :PluginInstall or something like that (it tab completes)

% Add system applications:
% alacarte -> add /opt/eclipse/eclipse
%          -> add python ~/git/hashpass/hashpass-gui.py

% PIP packages (TODO add more?)
sudo pip install docopt

% Make all startup apps visible:
sudo sed -i 's/NoDisplay=true/NoDisplay=false/g' /etc/xdg/autostart/*.desktop
% Startup Applications -> redshift -O 5300K

% GUI Settings:
% System Settings ->
%  * Mouse -> Natural Scrolling
% Unity-Tweak-Tool ->
%  * Launcher -> 32px
%  * Icon background -> per workspace
%  * Search -> uncheck Search Online Resources
%  * Panel -> Transparency level -> 3/4
% 			Indicators -> Date & Weekday
% 			Display remaining battery life
%  * Workspace settings -> 6x2
%  * Window snapping -> all 4 corners
%  * Theme -> Numix, Icons -> Numix Circle
%  * Scrolling -> Legacy Scrollbars

% git (Only if you haven't copied keys from old install)
ssh-keygen -t rsa -C "seveneightn9ne@gmail.com"
% Put ssh key in github

sudo visudo -> NOPASSWD:ALL

% enable hibernate
sudo cp com.ubuntu.enable-hibernate.pkla /etc/polkit-1/localauthority/50-local.d/

% firewall
sudo ufw enable
