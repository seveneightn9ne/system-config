% Repositories:
sudo add-apt-repository "deb http://repository.spotify.com stable non-free"
% sudo add-apt-repository ppa:webupd8team/java
sudo add-apt-repository ppa:numix/ppa         % numix window theme
sudo add-apt-repository ppa:tualatrix/ppa     % ubuntu-tweak
sudo add-apt-repository ppa:ehoover/compholio % netflix
sudo add-apt-repository ppa:synapse-core/ppa  % synapse
sudo apt-get update

% This is for Spotify:
sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 94558F59

% Packages:
sudo apt-get install synapse eclipse spotify-client unity-tweak-tool nautilus-dropbox zsh git texlive python-sklearn latexmk texlive-science texlive-latex-extra numix-gtk-theme numix-icon-theme numix-icon-theme-circle numix-wallpaper-saucy redshift ubuntu-tweak indicator-multiload netflix-desktop

% If synapse is not yet available in 14.04 use ppa:synapse-core/testing

% Less awesome packages:
% krb5-user krb5-config 
% Kerberos default realm: ATHENA.MIT.EDU

% From web:
Sublime Text 3 	http://www.sublimetext.com/3
Sublime PM	https://sublime.wbond.net/installation
Chrome 		https://www.google.com/intl/en/chrome/browser/
oh-my-zsh	wget --no-check-certificate http://install.ohmyz.sh -O - | sh
( chsh -s /usr/bin/zsh ) 
spotify-notify	git clone https://github.com/sveint/spotify-notify.git spotify-notify
				chmod a+x spotify-notify.py

% Make all startup apps visible:
sudo sed -i 's/NoDisplay=true/NoDisplay=false/g' /etc/xdg/autostart/*.desktop

% GUI Settings:
System Settings -> 
 * Mouse -> Natural Scrolling
Unity-Tweak-Tool -> 
 * Launcher -> 32px
 			   Icon background -> per workspace 
 * Search -> uncheck Search Online Resources
 * Panel -> Transparency level -> 3/4
 			Indicators -> Date & Weekday
 			Display remaining battery life
 * Workspace settings -> 9x1
 * Window snapping -> all 4 corners
 * Theme -> Numix
Sublime ->
 * theme: LAZY
 * indent using spaces
 * Install -> LaTeXTools
 * LaTeXTools Settings -> traditional builder (try "simple" if it's not finding packages)
 						  forward_sync = false
Ubuntu Tweak ->
 * Disable HUD
Startup Applications ->
 * redshift -O 5300K
 * /home/jessk/spotify-notify/spotify-notify.py -s

% subl ~/.zshrc
ZSH_THEME="mh"
if [ -f ~/.aliases ]; then
   . ~/.aliases
fi

Synapse settings
 * Startup on login

% If Eclipse has no menu options:
sudo subl /usr/share/applications/eclipse.desktop
Change the line that says: Exec=eclipse
to: Exec=env UBUNTU_MENUPROXY=0 eclipse

% Disable bluetooth:
% This doesn't work... 
sudo echo "BLUETOOTH_ENABLED=0" >> /etc/default/bluetooth

sudo apt-get autoremove indicator-messages; killall indicator-messages-service

% git
ssh-keygen -t rsa -C "seveneightn9ne@gmail.com"
Put ssh key in github

sudo visudo -> NOPASSWD:ALL

% To get an athena ticket: 
kinit -p jessk
% to renew:
kinit -R
% For athena-based repos, use athena instead of jessk@athena.dialup.mit.edu as the host

% to get Netflix working
run `netflix` and follow all install instructions
